#wrapper
  .full-width-top
    == render 'shared/navigationbar', page: 'user' 
  
  
  #sidebar-wrapper
    ul class="sidebar-nav"
      == render 'users/shared/sidebar', selected: 'user'
  
  #page-content-wrapper
    .container-fluid
      h2 Not found
      .sexy_line
      .alert.alert-danger
        'The recording you are looking for do not exists
      .row
        .col-sm-12
          .not_found_1
      .row
        .col-sm-12
          .pull-right
            = link_to 'Image by Ava Verino', 'https://www.flickr.com/photos/avaverino/4734314432/'
      /.row
      /  .col-xs-12.col-sm-6.col-lg-4
      /    
      /    .digi-box    
      /      => link_to logout_path(redirect_to_message: @redirect_to_message) do
      /        .btn.btn-warning
      /          i class="fa fa-unlock"
      /          '&nbsp;
      /          'Yes lets go
      /      = link_to user_path(current_user)
      /        .btn.btn-default
      /          'No thanks
      /              
            
                 
              
  == render 'shared/site_footer'    
== render 'users/shared/mobile_links'