= simple_form_for [@account, @recording], url: url_for(action: 'create', controller: 'upload_recordings'), method: 'post' do |f|
  /=f.hidden_field :transloadit => :waveform_to_s3
  =f.input :audio_file
  
  
  
  

/<%= form_for @recording, :html => { :id => 'upload' } do |form| %>
/  <%= transloadit :waveform_to_s3 %>
/  <%= form.label      :file, 'File to upload' %>
/  <%= form.file_field :file %>
/  <%= form.submit %>
/<% end %>
/
/<%= transloadit_jquerify :upload, :wait => true %>
