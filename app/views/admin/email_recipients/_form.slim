/= simple_form_for [:admin, @email_group] do |f|
= simple_form_for [:admin, @email_group], turboboost: true do |f|
  
  = f.input :recipients, autofocus:  true
  .row
    .col-sm-12
      .pull-right
        = f.submit 'Save', class: 'btn btn-primary' 