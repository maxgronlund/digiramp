#wrap
  .full-width-top
    == render 'admin/shared/navbar', page: 'users'

            
  .container
    ==render 'admin/shared/messages'
    == render 'shared/breadcrumbs', breadcrumbs: { links: { @account.title.to_sym => account_path(@account), \
                                                            assets: account_assets_path(@account), \
                                                            catalogs: account_catalogs_path(@account),\
                                                            @catalog.title.to_sym => account_work_path(@account, @catalog), \
                                                            }, current: 'invite user to catalog'}
    
    -blog = Blog.cached_find('Catalog Users')
    == render 'shared/page_title', locals: {blog: blog, blog_post: 'Edit Permissions'}
    .row
      .col-sm-2
      
      .col-sm-6
        .panel
          .panel-body
            =simple_form_for [@account, @catalog, @catalog_user] do |f|
              = f.hidden_field :account_id, value: @account.id
              = f.hidden_field :catalog_id, value: @catalog.id

              h3 = "#{@catalog_user.user.email} can: " 
              hr
              = f.input :can_edit, label: 'Can edit catalog'
              = f.input :edit_recordings
              = f.input :upload_recordings
              = f.input :read_works
              = f.input :edit_works
              = f.input :access_files
              = f.input :access_legal_documents
              = f.input :access_financial_documents
              = f.input :access_ipis
              = f.input :create_playlists
              
              .pull-right
                =f.submit 'Save', class: 'btn btn-primary btn-lg'
      .col-sm-3

    
    
-cache (['footer', Admin.cached_find(1).version]) do      
  =render 'shared/footer'