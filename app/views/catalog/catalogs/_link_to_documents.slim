li class="old-link-to-common-works #{'sidebar-selected' if selected == 'assets'}"
  = link_to catalog_account_catalog_assets_path(account, catalog) do
    i class="fa fa-folder"
    '&nbsp;
    = pluralize(catalog.documents.count, 'Document')