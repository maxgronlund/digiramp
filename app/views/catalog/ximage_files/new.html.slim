/= debug @recording.audio_upload
#wrap
  div class="#{'profile' if account_owner?}"
    .full-width-top
      == render 'admin/shared/navbar', page: 'account' 
  .do           
    .container
      == render 'admin/shared/messages'
      == render 'shared/breadcrumbs', breadcrumbs: { links: { @account.title.to_sym => account_path(@account), \
                                                              catalogs: catalog_account_catalogs_path(@account), \
                                                              @catalog.title.to_sym => catalog_account_catalog_path(@account, @catalog), \
                                                              recordings: catalog_account_catalog_recordings_path(@account, @catalog), \
                                                              @recording.title.to_sym => catalog_account_catalog_recording_path(@account, @catalog, @recording), \
                                                              }, current: 'Artwork'}
      
      
      
      == render 'shared/title', locals: {title: @recording.title, body: "UPLOAD ARTWORK"} 
      
      
      .row
        .col-sm-3
        .col-sm-6
          .panel.panel-primary
            .panel-heading
              'Select Files
            .panel-body

              = simple_form_for [ @account, @catalog, @recording, @image_file ], \
                                  :url => url_for(:action => 'create', \
                                  :controller => 'catalog/image_files'), \
                                  :method => 'post',  :html => { :id => 'image_file' } do |f| 
                
                
                = f.hidden_field :account_id, value: @account.id
                = f.hidden_field :catalog_id, value: @catalog.id
                = f.hidden_field :recording_id, value: @recording.id
                = transloadit :image_files_to_s3 

                = f.file_field :file, multiple: true
                hr
                .pull-right
                  = f.submit 'Done', class: 'btn btn-primary btn-lg'
            
            
              = transloadit_jquerify :image_file, :wait => true
        .col-sm-3
          
    
-cache (['footer', Admin.cached_find(1).version]) do      
  =render 'shared/footer'
              

            
 
          
          
          
        
