div class="old-recording-confirmation-button-#{recording_ipi.id.to_s}"
  - if recording_ipi.confirmation = 'Missing'
    = link_to 'Request confirmation', user_user_recording_recording_confirmation_path( user,  recording, recording_ipi), method: 'put', class: 'btn btn-default'
    
