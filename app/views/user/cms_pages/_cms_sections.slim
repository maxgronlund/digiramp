div id="old_cms_sections_#{column_nr}"
  - cms_page.cms_sections.order(:position).where(column_nr: column_nr).each do |section|
    == render 'user/cms_pages/' + section.view.view_name, section: section.view