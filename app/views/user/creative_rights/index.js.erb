if("<%= @remove_old_recordings %>"){
  $('.old-recordings').remove();
}
//$('.old-recordings').remove();
$('.add_recordings').append("<%= j render 'user/creative_rights/index', recordings: @recordings %>");