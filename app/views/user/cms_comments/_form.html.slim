= simple_form_for [:user, @user, @cms_comment] do |f|

  = f.input :position, as: :integer,  input_html: { value: @cms_comment.cms_section.position }

  .row
    .col-sm-12
      .pull-right
        = f.submit 'Save', class: 'btn btn-primary'