= simple_form_for [ @account, @recording ], :url => url_for(:action => 'add_genre', :controller => 'recordings'), :method => 'post',  :html => { :id => 'recording' } do |f| 
  = f.hidden_field :category, value: 'cinematic'
  
  h4 = @cinematic.title
  = f.submit "", :type => :image, :src => "#{@cinematic.image_url(:size_570x320)}", class: 'img-responsive'
  