/ ----------------------------------------
/ this is the public shop 
/-----------------------------------------
/= link_to 'New Product', new_user_user_product_path(@user)
#wrapper
  .full-width-top
    == render 'shared/navigationbar', page: 'home' 
  
  
  #sidebar-wrapper
    ul class="sidebar-nav"
      == render 'shop/shared/sidebar', selected: 'products'   

    
  
  
  #page-content-wrapper
    .container-fluid
      h2 Marketplace
      .sexy_line
      .products
        - @products.each do |product|
          == render 'shop/products/shared/badge', product: product, link_to_product: shop_product_path(product)
      