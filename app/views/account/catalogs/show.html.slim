
#wrap
  div class="#{'profile' if account_owner?}"
    .full-width-top
      == render 'admin/shared/navbar', page: 'account' 
  
  .do   
    .container
      div class="catalog_id" id="#{@catalog.id}"
      
      ==render 'admin/shared/messages'
      == render 'shared/breadcrumbs', breadcrumbs: { links: { @account.title.to_sym => account_path(@account), \
                                                              #assets: account_assets_path(@account), \
                                                              catalogs: account_account_catalogs_path(@account), \
                                                              }, current: @catalog.title}
      
                                                              
      - insert = current_account_user.update_catalog ? 'edit' : nil
      == render 'shared/page_title_text', locals: {title: @catalog.title, body: @catalog.body, insert: insert}
      
      .row
        .col-sm-6
          br
          .panel.panel-primary
            .panel-heading
              'Info
            .panel-body
              .table-responsive
                table class="table table-hover"
                  thead
                    tr
                      th Recordings:
                      th = @catalog.recordings.size
                    tr
                      th Works:
                      th = @catalog.common_works.size
                    tr
                      th Belongs to:
                      th = @catalog.account.title

                      
        .col-sm-6

          /= debug current_account_user  @catalog
          - if current_account_user.read_recording 
            == render 'badge_button', locals: { \
                                                 blog_post: 'RECORDINGS', \
                                                 link: account_account_catalog_recordings_path(@account, @catalog), \
                                                 css_class: 'btn btn-warning', \
                                                 icon: 'fa fa-headphones'\
                                               }
          
          /- if current_account_user.read_common_work
          /  == render 'badge_button', locals: { \
          /                                       blog_post: 'COMMON WORKS', \
          /                                       link: account_account_catalog_common_works_path(@account, @catalog), \
          /                                       css_class: 'btn btn-primary', \
          /                                       icon: 'fa fa-music'\
          /                                     }
          /
          /-if current_account_user.access_assets?  
          /  == render 'badge_button', locals: { \
          /                                       blog_post: 'ASSETS', \
          /                                       link: account_account_catalog_assets_path(@account, @catalog), \
          /                                       css_class: 'btn btn-success', \
          /                                       icon: 'fa fa-folder-o' \
          /                                     }
          /-if current_account_user.read_user? 
          /  == render 'badge_button', locals: { \
          /                                       blog_post: 'USERS', \
          /                                       link: account_account_catalog_catalog_users_path(@account, @catalog), \
          /                                       css_class: 'btn btn-default', \
          /                                       icon: 'fa fa-group' \
          /                                     }
          /                                     
          /-if can_edit? || current_user.id == @account.administrator.id 
          /  == render 'badge_button', locals: { \
          /                                       blog_post: 'MOVE CATALOG', \
          /                                       link: account_account_catalog_move_path(@account, @catalog), \
          /                                       css_class: 'btn btn-danger', \
          /                                       icon: 'fa fa-truck' \
          /                                     }
        /.col-sm-6
        /  == render 'badge_button', locals: { \
        /                                       blog_post: 'ADD DOCUMENTS', \
        /                                       link: account_add_catalog_asset_path(@account, @catalog), \
        /                                       css_class: 'btn btn-danger', \
        /                                       icon: 'fa fa-folder-o' \
        /                                     }
        /                                     
          
                                             
    
-cache (['footer', Admin.cached_find(1).version]) do      
  =render 'shared/footer'