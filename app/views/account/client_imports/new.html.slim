- blog = Blog.cached_find('Account Clients')
#wrap
  div class="#{'profile' if account_owner?}"
    .full-width-top
      == render 'admin/shared/navbar', page: 'account' 
  .do           
    .container
      == render 'admin/shared/messages'
      == render 'shared/breadcrumbs', breadcrumbs: { links: { @account.title.to_sym => account_path(@account), \
                                                              manage: account_account_path(@account), \
                                                              artwork: account_account_clients_path(@account) \
                                                             }, \
                                                      current:  'upload'}

      == render 'shared/page_title', locals: {blog: blog, blog_post: 'Upload Client CSV File'}
      
      
      .row
        .col-sm-3
        .col-sm-6
          .panel.panel-primary
            .panel-heading
              'Select CSV File
            .panel-body

              = simple_form_for [ :account,  @account, @client_import] do |f| 
                = f.hidden_field :account_id, value: @account.id

                = f.file_field :file
                hr
                .pull-right
                  = link_to 'Cancel', account_account_clients_path(@account), 'data-no-turbolink'=>'true', class: 'btn btn-default btn-lg'
                  '&nbsp;
                  = f.submit 'Done', class: 'btn btn-primary btn-lg'

        .col-sm-3
          
  == render 'shared/back', locals: {link: account_account_clients_path(@account), no_turbo: 'no_turbo'}
-cache (['footer', Admin.cached_find(1).version]) do      
  =render 'shared/footer'
              


